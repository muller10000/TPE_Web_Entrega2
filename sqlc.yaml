version: "2"
sql:
  - engine: "postgresql"
    schema: "database/schema.sql"
    queries: "database/queries.sql"
    gen:
      go:
        package: "repository"   # nombre del paquete Go generado
        out: "repository"       # carpeta donde se generará el código